**Khukuri** is an AI-powered virtual lab for antibiotic/drug discovery. Here's a summary:

**What it does:** End-to-end drug discovery pipeline, from target identification to synthesis planning, with a focus on antibiotics (the example uses tuberculosis targets *inhA* and *katG*).

**9 core modules:**
- **Target Discovery** — protein-protein interaction network analysis, literature mining
- **Molecule Design** — AI-generated molecules, fragment-based design, property optimization
- **Molecular Docking** — AutoDock Vina integration for binding pose analysis
- **ADMET Prediction** — drug-likeness, toxicity, pharmacokinetics
- **Resistance Prediction** — multi-target strategies, evolution simulation
- **Retrosynthesis** — route planning and synthetic accessibility scoring
- **Multi-Agent System** — autonomous AI agents that orchestrate the full workflow

**Stack:** Python (97%), built on RDKit, BioPython, NetworkX, with optional OpenAI integration for the AI agents.

**Status:** Work in progress, no releases yet, sole contributor.

Is this your project? Happy to help with code review, documentation, feature ideas, or anything else.

----

yes, mine. been dormant for months. now's time to work on it.

That's exciting — drug discovery tooling is a great space to build in. Before diving in, it'd help to know where you want to take it.

Q: What's your main goal for picking it back up?
A: Add new features

Q: Which module feels most important to focus on first?
A: The multi-agent orchestration system

----

try cloning the repo and read it.

Let me clone the repo and read through it.Good, I have a thorough picture of the codebase now. Here's what I see:

**What's already built and solid:**
- `HybridOrchestrator` is the main brain — it runs team meetings → workflow design → execution → analysis in a clean 6-phase pipeline
- `PIAgent` handles team creation + meeting facilitation (with GPT-4o-mini)
- `MeetingSystem` supports team meetings, individual agent meetings, and parallel meetings
- `FailureAnalyzer` learns from compound failures and builds a constraint database
- `CompetitiveTeams` runs multiple strategy teams against each other
- `ExplainableAI` generates human-readable reports
- Specialized agents exist: `MicrobiologyAgent`, `GenomicsAgent`, `CheminformaticsAgent`, plus `ChemistAgent`, `BiologistAgent`, `ToxicologistAgent`

**Key gaps / friction points I spotted:**

1. **`BaseAgent` is hardwired to OpenAI** (`gpt-3.5-turbo`) but the orchestrator optionally supports it — no support for Anthropic/Claude, Gemini, or local models
2. **No persistent memory** — each `run_discovery()` call starts fresh; `FailureAnalyzer`'s learned patterns are lost between sessions
3. **No async/parallel execution** — the orchestrator runs phases sequentially; the `parallel_meetings()` method exists but just loops synchronously
4. **`specialized_agents.py` uses a different, older `BaseAgent` API** (`name=`, `api_key=`, `_call_llm()`) — it's incompatible with the current `BaseAgent` and would break at runtime
5. **No agent-to-agent communication** — agents only speak through the PI; there's no direct peer debate
6. **World model exists** (`src/world_model/`) but isn't wired into the orchestrator at all

What feature are you most drawn to adding? Some natural next directions would be: multi-LLM support (so it works without an OpenAI key), persistent memory between runs, or fixing the broken `specialized_agents.py` to unlock those domain experts properly.


----

Why not all 6?! Lets make it happen.

Love the energy. Let me plan this properly before writing a single line.Good — I have everything I need. Let me now implement all 6 features systematically.Now I have a complete picture. Let me build all 6 features:Now let's update the `BaseAgent` and `HybridOrchestrator` to wire everything together:Now wire all 6 features into a new upgraded orchestrator:Now update `__init__.py` to export everything:Now verify everything imports cleanly:All green. Let me copy the new files to outputs:All 6 features implemented and smoke-tested. Here's what landed:

**8 new/updated files in `src/agents/`:**

| File | Feature |
|---|---|
| `llm_provider.py` | **#1 Multi-LLM** — `AnthropicProvider`, `OpenAIProvider`, `GeminiProvider`, `OllamaProvider`, `FallbackProvider`, `create_provider("auto")` |
| `persistent_memory.py` | **#2 Persistence** — JSON-backed key-value store at `~/.khukuri/memory/<project>/`, survives across sessions |
| `parallel_executor.py` | **#3 Async/parallel** — `ParallelExecutor` (thread pool) + `AsyncExecutor` (asyncio), dependency-aware task graph, progress reporting |
| `domain_agents.py` | **#4 Fixed specialists** — 7 properly wired domain agents (Chemist, Biologist, Toxicologist, Microbiologist, Genomics, Cheminformatics, Clinical), all using `LLMProvider` |
| `peer_debate.py` | **#5 Peer debate** — `PeerDebate` (2-agent), `PanelDiscussion` (N-agent round-robin), `SocraticDialog` (probe & refine) |
| `orchestrator.py` | **#6 World model** wired in + all 5 above integrated into `KhukuriOrchestrator` |
| `base_agent.py` | Updated — now accepts any `LLMProvider`, still backwards-compatible with `openai_client=` |
| `__init__.py` | Updated exports |

**Drop-in usage:**
```python
# Auto-detects key from env (ANTHROPIC_API_KEY, OPENAI_API_KEY, etc.)
orch = KhukuriOrchestrator()
results = orch.run_discovery("Staphylococcus aureus")

# Explicit provider
from src.agents import AnthropicProvider, KhukuriOrchestrator
orch = KhukuriOrchestrator(provider=AnthropicProvider())

# Old code still works unchanged
orch = HybridOrchestrator(openai_client=client)
```






